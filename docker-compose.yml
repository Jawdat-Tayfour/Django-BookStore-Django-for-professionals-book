version: "3.9"

services:
  web:
    build : .
    command : gunicorn django_project.wsgi -b 0.0.0.0:8000 
    
    volumes:
      - .:/code
    ports:
      - 8000:8000
    depends_on:
      - db 
    environment:
      - DJANGO_SECRET_KEY="django-insecure-aj@au6kgm@&5$$*7w1eh5p&!_yq#jndq+npouhny+4b59*n@lrz"
      - DJANGO_DEBUG=True
  db:
    image: postgres:13
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      - "POSTGRES_HOST_AUTH_METHOD=trust"
volumes:
  postgres_data:
